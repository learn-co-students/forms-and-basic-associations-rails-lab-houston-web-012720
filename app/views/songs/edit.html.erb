<h1>Editing <%= @song.title %></h1>
<%= form_for @song  do |f| %>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.collection_select :genre_id,  @genrs, :id , :name %>
  <input name="song[note_ids][]">
  <input name="song[note_ids][]">
  <input name="song[note_ids][]">
  <br />
  <%= f.submit %>
<% end %>

